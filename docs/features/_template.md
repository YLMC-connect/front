# <도메인명>

> 마지막 갱신: YYYY-MM-DD | 상태: ⬜ 예정 / 🔵 진행중 / ✅ 완료 | 담당 Phase: P? | 진행률: NN%

> **사용법**: 새 도메인 작업 첫 진입 시 본 파일을 `<도메인>.md` 로 복사 → 헤더의 `<도메인명>` 교체 → 상단 메타 채움 → 작업 진행하며 ✅ 완료 / 결정 사항 / 미결 누적.
>
> **단일 출처 분리**: 본 페이지는 “도메인의 컨텍스트” 만 담습니다. **현재 진행/예정 작업 항목은 [../TASKS.md](../TASKS.md) 가 단일 출처** 이며, 본 페이지에 중복 기재하지 않습니다.

## 한 줄 요약
이 도메인이 사용자에게 제공하는 가치 한 줄.

---

## ✅ 완료
> AI 의 Pass 0/1 에서는 본 섹션을 **스킵** 합니다. 결과물 재사용 트리거가 있을 때만 본문 정독.
> 끝난 작업의 결과만 짧게. 상세 변경 이력은 [../LOG.md](../LOG.md).

- (예) 데이터 타입 정의 — `src/types/<area>.ts`
- (예) Mock 데이터 작성 — `src/mocks/<area>.ts`

---

## 주요 파일 (도메인 파일 지도)

| 경로 | 역할 |
|---|---|
| `app/(tabs)/<area>/index.tsx` | 목록 화면 |
| `app/(tabs)/<area>/[id].tsx` | 상세 화면 |
| `app/modal/<area>-new.tsx` | 등록 모달 |
| `src/services/<area>Service.ts` | 데이터 페칭 |
| `src/hooks/use<Area>.ts` | TanStack Query 훅 |
| `src/mocks/<area>.ts` | Mock 데이터 |
| `src/types/<area>.ts` | 타입 정의 |

> 파일별 상태(완료/진행/예정)는 본 표에 표기하지 않습니다 — 각 파일을 만드는 작업의 진행 상태는 TASKS.md 가 추적합니다. 본 표는 “이 도메인의 파일 구성” 지도 역할.

## 데이터 타입
[../../PLAN.md](../../PLAN.md) “🗃 데이터 타입 설계 > <도메인>” 참조. 변경 발생 시 본 섹션에 diff 를 남기고 PLAN.md 도 함께 갱신.

## 결정 사항 (최신 위)
- (YYYY-MM-DD) **<결정 제목>** — 배경 / 결정 / 영향 한 줄씩

> 도메인 내부 결정은 본 섹션에 누적. 여러 도메인/시스템 전반 영향 결정은 [../adr/](../adr/) 의 ADR 로 작성.

## 미결 / 추적
- (없음)

## 의존성
- (예) common 도메인의 queryClient · queryKeys · types/common · secureStore 사용

## 관련 ADR
- (없음)
